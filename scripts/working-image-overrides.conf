# Image override configuration for CSM offline bundle
# Format: image_name=full_registry_path:tag (clean format - just image name on left)
# Comments start with # and empty lines are ignored

#csi-isilon=my-registry.example.com/csi-isilon:nightly
