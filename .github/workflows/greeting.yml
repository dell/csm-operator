# This workflow is auto comment on first time issue or pull request contribution.
#
# Do not edit this file
# Mandated by https://eos2git.cec.lab.emc.com/CSW/innersource
name: Innersource Greetings
on:  # yamllint disable-line rule:truthy
  workflow_dispatch:
  issues:
    types: [opened]
  pull_request:
    types: [opened]
jobs:
  call:
    # See https://github.com/marketplace/actions/first-interaction
    uses: CSW/actions/.github/workflows/greeting.yml@main
    secrets: inherit
