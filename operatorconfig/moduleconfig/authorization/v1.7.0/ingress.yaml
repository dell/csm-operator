apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: proxy-server
  namespace: <NAMESPACE>
spec:
  ingressClassName: <PROXY_INGRESS_CLASSNAME>
  tls:
  - hosts:
      - <AUTHORIZATION_HOSTNAME>
      - <PROXY_INGRESS_HOST>
    secretName: karavi-auth-tls
  rules:
  - host: <AUTHORIZATION_HOSTNAME>
    http:
      paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: proxy-server
              port:
                number: 8080
  - host: <PROXY_INGRESS_HOST>
    http:
      paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: proxy-server
              port:
                number: 8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: role-service
  namespace: <NAMESPACE>
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "GRPC"
spec:
  ingressClassName: <ROLE_INGRESS_CLASSNAME>
  tls:
  - hosts:
      - role.<AUTHORIZATION_HOSTNAME>
    secretName: karavi-auth-tls
  rules:
  - host: role.<AUTHORIZATION_HOSTNAME>
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: role-service
            port:
              number: 50051