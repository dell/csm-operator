apiVersion: v1
connections:
  - objectscale:
      id: ecs1
      credentials:
        username: REPLACE_USER
        password: REPLACE_PASS
      namespace: REPLACE_NAMESPACE
      mgmt-endpoint: REPLACE_MGMT_ENDPOINT
      region: us-east-1
      emptyBucket: true
      protocols:
        s3:
          endpoint: REPLACE_S3_ENDPOINT
      tls:
        insecure: true
